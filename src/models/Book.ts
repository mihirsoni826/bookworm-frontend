export default interface Book {
    isbn: string;
    title: string;
    author: string;
    rating: number;
    imageUrl: string;
    price: number;
    listName: string;
    encodedListName: string;
    favourite: boolean;
}
